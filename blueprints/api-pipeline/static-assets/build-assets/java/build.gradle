plugins {
    id 'java'
    id 'maven-publish'
}

group = '{{{ javaNamespace }}}'
version = hasProperty('version') ? project.version : '1.0.0-SNAPSHOT'

sourceSets.main.java.srcDirs = ['src/main/java']

{{{ publishing }}}
